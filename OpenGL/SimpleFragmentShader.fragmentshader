#version 330

in vec3 retColor; // MUST use the same name as out variable used by vertex shader
in vec2 retTexCoord;

out vec4 FragColor; //this could be a problem

uniform sampler2D sampler1;



void main() 
{
	FragColor = texture(sampler1, retTexCoord); // Example 1: Texture
}